import { kv } from '@vercel/kv';
export default async function handler(request, response) { if (request.method !== 'POST') return response.status(405).json({ message: 'Method Not Allowed' }); try { const { username, newRole } = request.body; if (!username || !newRole) return response.status(400).json({ message: "Username and new role are required." }); if (!['free', 'member', 'admin'].includes(newRole)) return response.status(400).json({ message: "Invalid role specified." }); const user = await kv.get(`user:${username}`); if (!user) return response.status(404).json({ message: "User not found." }); user.role = newRole; await kv.set(`user:${username}`, user); return response.status(200).json({ message: `User "${username}" role updated to ${newRole}.` }); } catch (error) { console.error("Change role error:", error); return response.status(500).json({ message: "Failed to change user role." }); } }